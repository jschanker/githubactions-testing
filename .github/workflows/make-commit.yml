# https://stackoverflow.com/a/58393457
name: Push commit
on:
  push
jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Make commit with data
        run: |
          curl https://api.merakilearn.org/pathways?courseType=json > report2.txt
      - name: Commit report
        run: |
          git config --global user.name 'Your Name'
          git config --global user.email 'jschanker@users.noreply.github.com'
          git add report2.txt
          git commit -am "Testing"
          git push
